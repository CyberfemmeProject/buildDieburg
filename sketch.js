let dieburg; 
let x = 0;

function preload() {
  dieburg = loadImage('dieburg.png')
}

function setup() {
  createCanvas(500, 500);
}
  function draw() {
    background(220);
  image(dieburg, 0, 0);
  }